import english from './en';
import french from './fr';

type Translation = {
  PageSize: string;
  PageOrientation: string;
  Format: string;
  DPI: string;
  Generate: string;
}

export { english, french, Translation };
